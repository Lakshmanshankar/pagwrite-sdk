{
  "name": "pagewrite-workspace",
  "private": true,
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
