---
export interface Props {
  title?: string;
}

const { title = "Default Title" } = Astro.props;
---

<header class="pagewrite-header">
  <h1>{title}</h1>
  {/* Add OG support meta tags here if needed or through a layout */}
</header>
